import React from "react";
import { Container, Form, Title } from "./LoginForm.style";

export const LoginForm = () => {
  return (
    <Container>
      <Form>
        <Title>Log In</Title>
      </Form>
    </Container>
  );
};
